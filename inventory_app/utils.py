# Utility functions will be placed here
# For example, password hashing helpers (though Flask-Login and bcrypt handle much of this)
# or data validation functions.

# import bcrypt

# def hash_password(plain_text_password):
# """Hashes a password using bcrypt."""
# salt = bcrypt.gensalt()
# hashed_password = bcrypt.hashpw(plain_text_password.encode('utf-8'), salt)
# return hashed_password.decode('utf-8')

# def check_password(plain_text_password, hashed_password):
# """Checks a plain text password against a hashed one."""
# return bcrypt.checkpw(plain_text_password.encode('utf-8'), hashed_password.encode('utf-8'))

# This is a placeholder for now.
pass
